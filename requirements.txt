# requirements.txt for returnright
# The current send_new_images.py uses only Python standard library modules
# No external packages are required. Keep this file for future dependencies.

# Example of adding a package if needed in future:
# requests==2.31.0
# Pillow==10.0.0
